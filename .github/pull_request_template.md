## Work unit

- Implementation-plan step:
- Linked issue:
- This PR does not begin the following step: [ ]

## Outcome and user-visible behavior

Describe the completed behavior and its boundaries.

## Files changed

List the files and the purpose of each change.

## Verification

- [ ] `uv run ruff check .`
- [ ] `uv run ruff format --check .`
- [ ] `uv run mypy packages/core/src packages/server/src`
- [ ] `uv run pytest -q`
- [ ] `npm --prefix apps/web run lint`
- [ ] `npm --prefix apps/web run format:check`
- [ ] `npm --prefix apps/web run test`
- [ ] `npm --prefix apps/web run build`
- [ ] `npm --prefix apps/web audit --omit=dev`
- [ ] `git diff --check`

Record exact counts, skips, warnings, and any focused/adversarial probes:

## Scientific assumptions

State every introduced or changed assumption, or state `None`.

## Risks and blockers

State remaining risks and blockers, or state `None known for this work unit`.

## Plan and handoff

- Checklist items changed:
- Completion-log evidence added:
- Next smallest executable work unit:
- Monitor-owned completion checkbox remains unchecked: [ ]

## Review protocol

- [ ] PR is ready for monitored review, not a draft substitute for incomplete work.
- [ ] Required GitHub CI checks pass.
- [ ] No direct push, tag, release, deployment, or following-step work is included.
- [ ] Changes requested will be repaired on this same branch and PR.
